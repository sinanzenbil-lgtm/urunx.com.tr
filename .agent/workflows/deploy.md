---
description: Push changes to GitHub and deploy to Vercel
---
// turbo-all
1. Stage all changes
2. Commit changes
3. Push to main branch
   ```bash
   git add .
   git commit -m "Deploy updates: Fix modal freeze and logo color"
   git push origin main
   ```
